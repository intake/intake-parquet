plugins:
  source:
    - module: intake_fastparquet
sources:
  local:
    driver: fastparquet
    args:
      urlpath: !template '{{ CATALOG_DIR }}/local.parq'
  raw_live:
    driver: fastparquet
    args:
      urlpath: ~
