plugins:
  source:
    - module: intake_parquet
sources:
  local:
    driver: parquet
    args:
      urlpath: !template '{{ CATALOG_DIR }}/local.parq'
  raw_live:
    driver: parquet
    args:
      urlpath: ~
